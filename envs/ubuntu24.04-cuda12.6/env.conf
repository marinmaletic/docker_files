# Environment descriptor — read by build.sh and run.sh.
# Ubuntu 24.04 (Noble) + CUDA 12.6 runtime + cuDNN, NO ROS.
# For ML/vision/numerics work that doesn't need the ROS stack.

IMAGE=cuda-dev
TAG_FAMILY=u2404-cu126
PREFIX=dev_           # container name = ${PREFIX}${name}
HAS_ROS=0
