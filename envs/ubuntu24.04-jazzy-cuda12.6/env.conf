# Environment descriptor — read by build.sh and run.sh.
# Ubuntu 24.04 (Noble) + ROS2 Jazzy + CUDA 12.6 runtime + cuDNN

IMAGE=ros2-jazzy-cuda
TAG_FAMILY=u2404-cu126
PREFIX=ros2_          # container name = ${PREFIX}${name}
HAS_ROS=1             # run.sh sets ROS_DOMAIN_ID only when 1
